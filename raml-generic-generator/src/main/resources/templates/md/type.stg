model(type, builtInParent) ::= <<
### <type.name>

  <type.properties: {p|<property(p)>}>
>>

property(property) ::= <<
* `<property.name>` - <property.type.name> <if(property.required)> - Required<endif>
  <property.description>
>>
