apply plugin: 'maven-publish'

publishing {
    publications {
        Maven(MavenPublication) {
            from components.java
            artifactId 'functional-utils'

            artifact sourceJar {
                classifier 'sources'
            }
        }
    }
}

dependencies {
    testImplementation junit.junit
}
